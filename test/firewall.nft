#!/usr/sbin/nft -f

table inet test {

    chain test {

        # Chain configuration
        type filter hook ingress device enp0s8 priority 0; policy drop;

        # Rules
        meta l4proto tcp ct original proto-dst { 443 } accept

    }

}
